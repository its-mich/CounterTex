@{
    var user = Context.User; // Usuario actual

    if (user.IsInRole("Admin"))
    {
        Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    }
    else if (user.IsInRole("Empleado"))
    {
        Layout = "~/Views/Shared/_LayoutEmpleado.cshtml";
    }
}
